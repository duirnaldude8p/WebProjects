	# instance.user_id = validated_data['user_id']
						# instance.cat_name = validated_data['cat_name']
						# instance.breed = validated_data['breed']
						# instance.story = validated_data['story']
						# instance.cat_pic = validated_data['cat_pic']
						# recieved_id =  validated_data['get_id']
						# cat_comments = obj.cat_comment
						# obj.delete()
						# Cat.objects.create(
						# 						# user = user_id,
						# 						cat_name = cat_name,
						# 						breed = breed,	
						# 						story = story,
      #        									cat_pic = cat_pic,
      #        									section = 'updated',
      #        									category = 'cat',
      #        									cat_comments = ''
			 		# 					)